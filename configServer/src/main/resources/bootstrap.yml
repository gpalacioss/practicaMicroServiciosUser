# en el archivo bootstrap solo se configura el nombre de la aplicacion y la coneccion con el config server

#Nombre de la aplicacion como se mostrará en el eureka server 
# la url en donde se encuentra la configuracion

spring:
  application:
    name: config-server
  cloud:
     config:
       server:
         git:
          uri: https://github.com/gpalacioss/practicaMicroServiciosUser/archivosConfigServer
    
    
    
    
    
